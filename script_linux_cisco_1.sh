ls ~/Documents      #The command ls ~/Documents lists the contents of the Documents folder in your home directory.
aptitude        #Runs the Aptitude package manager in interactive mode. 
#It lets you search, install, upgrade, and remove software packages using a text-based interface.
aptitude moo        #This is a hidden Easter egg inside Aptitude. Instead of doing real package management, 
#it prints a funny message (usually something like “There are no Easter Eggs in this program” ).
ls -l       #The command ls -l lists files and directories in long format.
ls -r       #It shows the contents of a directory but reverses the sorting order (for example, Z → A instead of A → Z).
-v      #means verbose (more detailed output)
pwd     #It shows the current directory path you are in.  stands for “print working directory.”
cd      #It is used to move between directories in the filesystem.   stands for "change directory"
cd ..       #The command cd .. moves you to the parent directory.
cd ~        #The command cd ~ takes you to your home directory.
-t      #The option -t in the ls command sorts files by their timestamp (modification time).
-S      #The option -S in the ls command sorts files by their file size.
su -
su -l       #all three switch user and load a full login environment (like a real login session, by default root).
su --login
exit        #If you are inside another user (like after su), it returns you to the previous user.